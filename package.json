{
  "name": "apex-r9-webxr-drag-race",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22.13.0"
  },
  "scripts": {
    "dev": "vite",
    "build": "npm run export:car && vite build --config vite.pages.config.js",
    "build:pages": "npm run export:car && vite build --config vite.pages.config.js",
    "export:car": "node scripts/export-car.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "three": "0.180.0"
  },
  "devDependencies": {
    "vite": "8.0.13"
  }
}
